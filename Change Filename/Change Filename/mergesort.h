#pragma once

#include <string>
#include <vector>

void merge(std::vector<std::string>& s1, std::vector<std::string>& s2, int low, int pivot, int high);
void mergesort(std::vector<std::string>& s1, std::vector<std::string>& s2, int low, int high);